# Assignment #1 — HTML & CSS Basics

Name: Yermek Dinmukhambet  
Group: IT-2501  
University: Astana IT University  


 Introduction

This project is my first personal website created as part of the HTML and CSS Basics assignment.

The website contains information about me, my hobbies, favorite websites, images, a weekly schedule, and a contact form.



 Steps Completed

### Step 0 — Create a New HTML File

Created a new `index.html` file and added the basic HTML structure with `DOCTYPE`, `html`, `head`, `title`, and `body`.

### Step 1 — Structure Text Using HTML Tags

Added headings from `h1` to `h3` with my name, university information, and an "About Me" section. Also added a paragraph describing myself.

### Step 2 — HTML Lists

Created an ordered list containing my hobbies and an unordered list containing my favorite websites.

### Step 3 — Images and Links

Added an image to the webpage using the `img` tag and added clickable links to websites such as YouTube, Mangalib, and YouTube Music.

### Step 4 — HTML Buttons

Added a simple button with the text "Click Me".

### Step 5 — Tables

Created a weekly class schedule using an HTML table with three columns: Subject, Day, and Time.

### Step 6 — Using Tables for Layout

Created a two-column layout using a table. The left column contains the menu and lists, while the right column contains the main content.

### Step 7 — Typing Emojis

Added emojis to a paragraph about my hobbies:

🎮 🎧 🚲

### Step 8 — HTML Forms

Created a form with Name, Email, Favorite Color, and Submit button fields.

### Step 9 — Introduction to CSS

Started applying CSS styles to the HTML webpage to change colors, fonts, backgrounds, and other visual properties.

### Step 10 — Inline CSS

Used inline CSS to change the color of a paragraph directly inside the HTML element.

### Step 11 — Internal CSS

Used the `style` tag inside the HTML `head` section to create internal CSS rules.

### Step 12 — External CSS

Created a separate `style.css` file and connected it to the HTML document using the `link` tag.

### Step 13 — CSS Syntax and Selectors

Used element selectors, class selectors, and ID selectors to style different HTML elements.

### Step 14 — Classes vs. IDs

Created a `.highlight` class for styling multiple elements and an ID for styling the main heading.

### Step 15 — Favicons

Added a favicon to the website so that a small icon appears in the browser tab.

### Step 16 — HTML Divs

Used `div` elements to organize the webpage into three main sections: header, main content, and footer.

### Step 17 — CSS Box Model

Applied borders, margins, and padding to different elements. This helped control the spacing and size of the webpage sections.

### Step 18 — CSS Positioning

Used different CSS positioning methods:

- `static` — default element positioning.
- `relative` — shifted an element slightly from its normal position.
- `absolute` — positioned an element independently relative to its positioned parent.

### Step 19 — CSS Sizing

Used different CSS units such as `px`, `%`, `em`, and `rem` to control the sizes of headings, paragraphs, and images.

### Step 20 — Float and Clear

Created two boxes using `float: left` and `float: right`. Used `clear: both` to prevent overlapping with the floating elements.

### Step 21 — Publish the Website

Published the website using GitHub pages



